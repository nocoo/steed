# osv-scanner ignore list — currently empty after lockfile cleanup landed in
# v2026.5 base-ci. Re-add entries here only when osv-scanner reports a new
# vulnerability we have a documented, time-bound reason to suppress.
